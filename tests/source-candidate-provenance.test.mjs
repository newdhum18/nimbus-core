import test from "node:test";import assert from "node:assert/strict";import{readFile}from"node:fs/promises";import{isPermanentDiscoveryError}from"../src/sources/discovery-runs.js";test("provenance columns are separated",async()=>{const a=await readFile("src/sources/discovery.js","utf8"),b=await readFile("src/queue/consumer.js","utf8"),c=await readFile("src/sources/discovery-runs.js","utf8");assert.match(a,/discovered_from_autoscan_run_id,discovered_from_discovery_run_id/);assert.match(b,/autoscanRunId: current\.run_id/);assert.match(c,/discoveryRunId:task\.run_id/)});test("permanent DB errors are not retryable",()=>{assert.equal(isPermanentDiscoveryError(new Error("FOREIGN KEY constraint failed")),true);assert.equal(isPermanentDiscoveryError(new Error("fetch_timeout")),false)});