{
  "name": "nimbus-core-v36",
  "version": "36.15.0",
  "private": true,
  "type": "module",
  "description": "Nimbus Core V36.15 note-first recursive extraction, ranked nested targets, and deeper source discovery",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "test": "node --test tests/*.test.mjs",
    "build:web": "node scripts/build-web.mjs",
    "check:config": "node scripts/check-config.mjs",
    "check:release": "node scripts/check-release.mjs",
    "validate": "npm run clean && npm run check:config && npm test && npm run check:cloudflare && npm run check:runtime && npm run build:web && npm run baseline:verify",
    "validate:source": "npm run clean && npm run check:config && npm test",
    "predeploy:worker": "npm run validate && npm run check:release",
    "deploy:worker": "wrangler deploy --config wrangler.worker.jsonc",
    "deploy:worker:dry": "wrangler deploy --dry-run --outdir .wrangler/dry-run --config wrangler.worker.jsonc",
    "db:apply:local": "wrangler d1 migrations apply nimbus-core-v36-db --local --config wrangler.worker.jsonc",
    "db:apply:remote": "node scripts/apply-d1-migrations-remote.mjs",
    "check:cloudflare": "node scripts/validate-cloudflare.mjs",
    "check:runtime": "node scripts/validate-runtime.mjs",
    "db:test": "node --test tests/database-core.test.mjs",
    "db:migrate:local": "node scripts/db-migrate-local.mjs",
    "db:migrate:dry": "node scripts/db-migrate-local.mjs --dry-run",
    "db:inspect:local": "node scripts/db-inspect-local.mjs",
    "db:repair:local": "node scripts/db-repair-local.mjs",
    "baseline:verify": "node scripts/verify-baseline.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
